{%  extends 'base.html.twig' %}
{%  block body %}

    <ul>
        {% trans %}general.nonempty_list{% endtrans %}
        {%  for tag in tags %}
            <li>
                {{tag.title}}
            </li>
        {% else %}
            {% trans %}general.empty_list{% endtrans %}
        {%  endfor %}
    </ul>
{%  endblock %}